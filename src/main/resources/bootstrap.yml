spring:
  application:
    name: ch-gateway
  profiles:
    active: local
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.7:8848
        namespace: ${nacos.namespace:}
      config:
        server-addr: 192.168.1.7:8848
#        shared-dataids: ch-gateway.yml
        namespace: ${nacos.namespace:}