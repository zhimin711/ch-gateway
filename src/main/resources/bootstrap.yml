spring:
  application:
    name: ch-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 10.202.10.66:8848
        namespace: ${nacos.namespace:}
      config:
        server-addr: 10.202.10.66:8848
        shared-dataids: ch-gateway1.yml
        namespace: ${nacos.namespace:}